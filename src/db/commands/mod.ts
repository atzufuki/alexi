/**
 * Alexi DB Commands
 *
 * NOTE: Migration commands have been moved to @alexi/core to avoid circular
 * dependency between @alexi/db and @alexi/core.
 *
 * Import migration commands from @alexi/core instead:
 * ```ts
 * import {
 *   MigrateCommand,
 *   MakemigrationsCommand,
 *   ShowmigrationsCommand,
 *   SqlmigrateCommand,
 * } from "@alexi/core";
 * ```
 *
 * @module @alexi/db/commands
 * @deprecated Import migration commands from @alexi/core instead
 */

// Re-exports removed to break circular dependency.
// Migration commands are now in @alexi/core/commands/
